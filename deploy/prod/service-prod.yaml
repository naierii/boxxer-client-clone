apiVersion: v1
kind: Service
metadata:
  labels:
    app: boxxer-client
  name: boxxer-client
  namespace: default
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: boxxer-client
  type: ClusterIP
