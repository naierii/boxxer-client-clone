apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: boxxer-client-testing-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: letsencrypt-prod-dns
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.boxxerworld.com
      secretName: boxxer-client-testing-tls
  rules:
    - host: staging.boxxerworld.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: 
                name: boxxer-client-testing
                port: 
                  number: 8080
